<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nearest Doctors</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f0f0f0;
      }
      #btnn {
        margin-bottom: 2rem;
        background-color: green;
        font-size: 17px;
      }
      /* */
      #button:hover {
        background-color: rgb(10, 118, 10);
      }

      h1 {
        text-align: center;
        margin-bottom: 20px;
        color: #333;
      }

      form {
        text-align: center;
        margin-bottom: 20px;
      }

      label {
        font-weight: bold;
      }

      input[type="text"] {
        width: 80%;
        padding: 8px;
        margin: 8px 0;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
      }

      button {
        background-color: #007bff;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      ul {
        list-style-type: none;
        padding: 0;
      }

      li {
        background-color: #fff;
        border: 1px solid #ccc;
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 20px;
      }

      li strong {
        font-weight: bold;
      }

      li:nth-child(even) {
        background-color: #f9f9f9;
      }

      li:hover {
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body>
    <h1>Nearest Doctors</h1>
    <form action="/doctorNearby" method="post">
      <label for="address">Enter Address:</label>
      <input type="text" id="address" name="address" />
      <button type="submit">Find Doctors Nearby</button>
    </form>
    <ul>
      <% if (data){ %> <% data.forEach(doctor => { %>
      <li>
        <strong>Name:</strong> <%= doctor.name %><br />
        <strong>Address:</strong> <%= doctor.vicinity %><br />
        <strong>Rating:</strong> <%= doctor.rating %>
      </li>
      <button id="btnn">Book an Appointment</button>

      <% }); %> <% } %>
    </ul>
  </body>
</html>
